//this changes the players sprite and lets you adjust the speed and index of the image
function SpriteStates(sprite, aniSpeed, index){
		if sprite_index != argument0
		{
			sprite_index = argument0;
			image_speed = argument1;
			image_index = argument2;
		}
}